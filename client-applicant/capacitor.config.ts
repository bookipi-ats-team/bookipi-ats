import type { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'com.payroller.applicant',
  appName: 'bookipi-applicant',
  webDir: 'dist'
};

export default config;
